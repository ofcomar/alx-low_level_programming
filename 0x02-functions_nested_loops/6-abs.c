#include "main.h"
/**
 * abs - computes the absolute value of an integer
 *  n is an integer
 *
 * Return: integer
 */
int _abs(int)
{
		if (n > 0)
	{
		return (n);
	}
	else if (n < 0)
	{
		return (-n);
	}
	else
	{
		return (n);
	}
}
