#include "main.h"
/**
 * abs - computes the absolute value of an integer
 *  n is an integer
 *
 * Return: integer
 */
int _abs(int)
{
	if (d < 0)
	{
		int abs_v;

		abs_v = d * -1;
		return (abs_v);
	}
	return (d);
}
