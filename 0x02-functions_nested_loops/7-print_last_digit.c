#include "main.h"

/**
 * main - check the code
 *
 * Return: Always 0.
 */
int print_last_digit(int)
{
    int r;

    print_last_digit(n);
    r = print_last_digit(n);
    _putchar('0' + r);
    _putchar('\n');
    return (0);
}
