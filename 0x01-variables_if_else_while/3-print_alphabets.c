#include <stdio.h>
/**

 * main - Prints the alphabet in lowercase. and upper
 *
 * Return: 0 good girl
 */

int main (void)
{

        int n = 97 ;
        int u = 65 ;
	while(n <= 122)
	{
		putchar(n);
	}
	while(u <= 65)
	{
		putchar(u);
	}
	return(0);
}
