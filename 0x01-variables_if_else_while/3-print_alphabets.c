#include <stdio.h>
/**
 * main - Prints the alphabet in lowercase.
 *
 * Return: 0 good girl
 */
int main (void)
{
        int n = 97;

        while (n <= 122)
        {
                putchar(n);
                n++;
        }
        putchar('\n');
	 int u = 65;

        while (u <= 90)
        {
                putchar(u);
                n++;
        }
        putchar('\n');

        return (0);

}
